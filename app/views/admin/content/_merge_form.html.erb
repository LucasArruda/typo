<h3>Merge Articles</h3>

<%= form_tag({:action => "merge", :id => @article.id}, :id => "merge_form") do %> 
	<h4>Article Id</h4>
	<%= text_field_tag 'merge_with' %>
  <%= submit_tag 'Merge' %>
<% end %>